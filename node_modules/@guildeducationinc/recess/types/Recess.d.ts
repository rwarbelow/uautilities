export interface BaseProps {
    /**
     * CSS classes that are passed through to the component.
     * Otherwise noted, these classes are always applied to the root level component wrapper.
     */
    className?: string;
    /**
     * Required prop that can be used for selection of a component for things like tests.
     * Must be unique.
     */
    testid?: string;
}
