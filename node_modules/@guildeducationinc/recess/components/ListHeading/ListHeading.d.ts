import * as React from 'react';
export interface ListHeadingProps extends React.HTMLAttributes<HTMLElement> {
    /**
     * For optimum accessibility, provide an id for your list heading. This id will be reference via aria-labelledby on the
     * list container.
     */
    id?: string;
    /**
     * Sets the heading tag you want to use. Defaults to an `h5`.
     * Make sure this
     *
     * @default h5
     */
    tag?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
    /**
     * @deprecated use the `tag` prop instead
     */
    is?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
}
export declare const ListHeading: React.FunctionComponent<ListHeadingProps>;
