import * as React from 'react';
export interface SliderProps {
    className?: string;
    id: string;
    /**
     * Whether the slider is open/visible.
     */
    isOpen: boolean;
    /**
     * Callback for when slider is closed by clicking overlay. If you don't want the Slider to close on overlay click, leave onClose undefined.
     */
    onClose?: () => void;
    /**
     * Content rendered in Slider
     */
    sliderContent: React.ReactNode;
    testid?: string;
    /**
     * Number of pixels offsetting slider from top of container.
     *
     * @default 0
     */
    topOffset?: number;
}
export declare const Slider: React.SFC<SliderProps>;
