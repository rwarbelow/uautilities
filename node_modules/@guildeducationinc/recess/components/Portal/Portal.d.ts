import * as React from 'react';
export interface PortalProps<P> {
    /**
     * Single element root that will serve as the container for the portal content
     */
    Content: React.ComponentType<P>;
    /**
     * Props to be passed down to the content component when rendered
     */
    contentProps: P;
}
/**
 * Portals allow for React content to be rendered into an arbitrary container. The Portal component
 * composes ContainerPortal and uses the document body as the container to render into
 */
export declare function Portal<P>(props: PortalProps<P>): JSX.Element;
