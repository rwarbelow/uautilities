import * as React from 'react';
import { IconTypes } from '../Icons/IconTypes';
export interface LinkPropMapperType {
    /**
     * Add any additional classNames to the component
     */
    className?: string;
    /**
     * Add if the link is active and needs proper styling
     */
    active?: boolean;
    /**
     * Passed to dictate which Icon to use in the link.
     */
    iconName?: IconTypes;
    /**
     * Passed to dictate which color of Icon to use in the button.
     * Can be color string or hex value.
     */
    iconColor?: string;
    /**
     * The label/title of the button which can only be a string.
     */
    title?: string;
    children?: React.ReactNode;
}
interface ExternalLinkProps extends React.HTMLProps<HTMLAnchorElement>, LinkPropMapperType {
    /**
     * The url of the anchor tags destination.
     */
    href: string;
}
export declare const mapLinkProps: (props: LinkPropMapperType) => {
    classes: string;
    computedChildren: JSX.Element;
};
/**
 * @deprecated [#1] since version 4.0.11 [#2] Use Button instead
 * [#3] Will be removed in version 5.0.
 *
 * ExternalLink Component. Visually identical to the Link component, but does not use react-router-dom
 *
 * @example
 * <ExternalLink
 *   href={'https://www.guildeducation.com}
 *   title="external link button"
 *   active
 *   key={`HeaderNavItem-${1}`}
 *   iconName="ArrowLeft" // If you want an icon in front of the link
 * />
 *
 * <ExternalLink href={'https://www.guildeducation.com'} key={`navItem-${1}`}>
 *  link with text child
 * </Link>
 */
export declare const ExternalLink: React.SFC<ExternalLinkProps>;
export {};
