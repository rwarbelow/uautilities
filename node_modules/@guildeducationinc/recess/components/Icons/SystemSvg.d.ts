import * as React from 'react';
export interface IconProps extends React.SVGAttributes<SVGSVGElement> {
    /**
     * Any custom class
     */
    className?: string;
    children?: React.ReactNode;
    /**
     * Affects currentColor which is applied to SVG fill and
     * may be applied to other common attributes that accept currentColor
     * @default currentColor
     */
    color?: string;
    /**
     * Add a description (if helpful) to improve accessibility
     */
    desc?: string;
    /**
     * May be modified to affect SVG frame dimensions
     * @default 0 0 24 24
     */
    viewBox?: string;
    /**
     * Affects accessibility properties based on intended use
     *
     * @default standalone
     */
    intent?: 'standalone' | 'decorative';
    /**
     * Number of pixels; Applied to height and width.
     * @default 16
     */
    size?: number;
    onClick?: (event: React.MouseEvent<SVGSVGElement, MouseEvent>) => void;
}
declare const SystemSvg: React.SFC<IconProps>;
export default SystemSvg;
