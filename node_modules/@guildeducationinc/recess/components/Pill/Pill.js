import * as React from 'react';
import { linkTo } from '@storybook/addon-links';
import classnames from 'classnames';
import * as styles from './index.module.css.js';
export var Pill = function (_a) {
    var _b;
    var children = _a.children, className = _a.className, componentName = _a.componentName, intent = _a.intent, story = _a.story;
    var onClick;
    var label;
    if (intent === 'themeable') {
        onClick = linkTo('Utilities|Theming');
        label = 'Themeable';
    }
    else if (intent === 'deprecated') {
        onClick = linkTo('Utilities|Deprecated');
        label = 'Deprecated';
    }
    else {
        onClick = linkTo(componentName, story);
    }
    return (React.createElement("button", { onClick: onClick, className: classnames(styles.pill, (_b = {},
            _b[styles.themeable] = intent === 'themeable',
            _b[styles.deprecated] = intent === 'deprecated',
            _b.className = className,
            _b)) }, intent ? label : children));
};
