import * as React from 'react';
export interface ErrorListProps {
    /**
     * Array of error message strings, or single string (for a single error). If array has more than one error, they will be displayed in an unordered list.
     */
    errors: string[] | string;
    /**
     * Whether the errors pertain to a "global" state (i.e. full form rather than single input).
     *
     * @default false
     */
    global?: boolean;
    /**
     * Id that is used to connect invalid fields with error. Important for accessiility.
     */
    id?: string;
    /**
     * Whether the error list should be rendered.
     */
    show: boolean;
}
export declare const ErrorList: React.SFC<ErrorListProps>;
