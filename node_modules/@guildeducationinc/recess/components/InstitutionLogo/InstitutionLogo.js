import React from 'react';
import classNames from '../../utilities/classnames';
import { detectIE } from '../../utilities/detectIE';
function InstitutionLogoComponent(props) {
    var institutionName = props.institutionName, _a = props.colorScheme, colorScheme = _a === void 0 ? 'color' : _a, extension = props.extension;
    var pathExtension = extension || (detectIE() ? 'png' : 'svg');
    var imagePath = "https://recess-images.imgix.net/learning_partner_logos/" + colorScheme + "/" + institutionName + "_" + colorScheme + "." + pathExtension;
    var classname = classNames('institution-logo', institutionName, props.className);
    return React.createElement("img", { className: classname, src: imagePath, alt: institutionName + "-logo" });
}
/**
 * Commented out because this breaks typescript. Leaving in for reference, but please convert to a typescript file.
 */
// InstitutionLogoComponent.propTypes = {
//   /** Used to identify institution as part of img url construction */
//   institutionName: PropTypes.string,
//   /** Used to identify color ('color', 'gray', 'white') as part of img url construction */
//   colorScheme: PropTypes.string,
//   /** Used to identify extension ('svg' or 'png') as part of img url construction; 'png' is fallback for IE */
//   extension: PropTypes.string,
// }
export var InstitutionLogo = InstitutionLogoComponent;
