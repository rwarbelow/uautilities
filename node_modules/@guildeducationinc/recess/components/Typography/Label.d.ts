import * as React from 'react';
export interface LabelProps {
    /**
     * Custom className to apply to label root element
     */
    className?: string;
    /**
     * Use the `RecessColors` map from `constants/colors` to assign a specific color to your Label. Defaults to `RecessColors.black.base`.
     *
     * @default RecessColors.black.base
     */
    color?: string;
    /**
     * When `Label` is used for input help text, you should assign Label an id and add `aria-describedby`
     * to your input with the value of this id for accessibility.
     */
    id?: string;
    style?: React.CSSProperties;
    /**
     * String that will be used as a data-testid attribute for automation selection
     */
    testid?: string;
    /**
     * Optional tag to use as root of Label element.
     *
     * @default p
     */
    tag?: keyof React.ReactHTML;
    /**
     * Allows you to set the text color of your label for theming purposes.
     * The value should correlate with the theme of the parent.
     * Defaults to `light` (i.e. black text color).
     *
     * @default light
     */
    theme?: 'light' | 'dark';
    /**
     * Variation 1 is slightly denser than variation 2. See examples and specs.
     */
    variation?: 1 | 2;
    /**
     * Sets the font-weight of Label. Defaults to `600`.
     *
     * @default 600
     */
    weight?: '600' | 'normal';
}
export declare const Label: React.FC<LabelProps>;
