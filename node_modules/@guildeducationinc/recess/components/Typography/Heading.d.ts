import * as React from 'react';
export interface HeadingProps {
    /**
     * Custom class applied to the header element
     */
    className?: string;
    /**
     * Use the `RecessColors` map from `constants/colors` to assign a specific color to your heading. Defaults to `RecessColors.black.base`.
     *
     * @default RecessColors.black.base
     */
    color?: string;
    /**
     * Optional id you can pass to your heading if you need to reference it elsewhere. I.E. aria-labelledby
     */
    id?: string;
    /**
     * Heading `size` affects the font size and the default HTML tag,
     * although the tag can be overridden by using the `tag` prop.
     * If the `tag` is not specified, a logical default tag is mapped to the value of the `size` prop.
     * For example, a `size` of `1` would map to a 'h1' tag.
     *
     * Please use numerical values 1 - 6 moving forward.
     *
     * String values `'100'` - `'600'` are deprecated and will be removed in the next release.
     *
     * When updating, note that `'600'` maps to `1`, `'500'` maps to `2`, etc.
     * `'250'` maps to `2`
     *
     * @default 3
     */
    size?: 1 | 2 | 3 | 4 | 5 | 6 | '100' | '200' | '250' | '300' | '400' | '500' | '600';
    style?: React.CSSProperties;
    /**
     * Semantic HTML heading tag to be used. If `tag` is not specified,
     * a default HTML tag is derived from value of the `size` prop.
     *
     * For example, a `size` of `1` would map to an `h1` tag.
     * Default is `h3` because `size` prop default is `3`.
     *
     * @default h3
     */
    tag?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
    /**
     * String that will be used as a data-testid attribute for automation selection
     */
    testid?: string;
    /**
     * Allows you to set the text color of your headings for theming purposes.
     * The value should correlate with the theme of the parent.
     * Defaults to `light` (i.e. black text color).
     *
     * @default light
     */
    theme?: 'light' | 'dark';
    /**
     * Sets the font-weight of Heading. Defaults to `800`.
     *
     * @default 800
     */
    weight?: '800' | 'normal';
}
export declare const Heading: React.FC<HeadingProps>;
