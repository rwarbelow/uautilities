import * as React from 'react';
import * as styles from './index.module.css.js';
import classnames from '../../utilities/classnames';
import { ThemeContext } from '../Theme';
var sizeMap = {
    '100': '',
    '150': '',
    '200': 'Large',
    '250': 'Large',
    large: 'Large',
};
export var Paragraph = function (_a) {
    var size = _a.size, tag = _a.tag, testid = _a.testid, style = _a.style, className = _a.className, children = _a.children, theme = _a.theme;
    var contextTheme = React.useContext(ThemeContext);
    var computedTheme = theme || (contextTheme === null || contextTheme === void 0 ? void 0 : contextTheme.theme) || 'light';
    var sizeLabel = sizeMap[size];
    var classes = classnames(styles.paragraph, styles["paragraph" + sizeLabel], styles[computedTheme], className);
    var TagName = (tag || 'p');
    return (React.createElement(TagName, { style: style, className: classes, "data-testid": testid }, children));
};
