import * as React from 'react';
export interface ParagraphProps {
    className?: string;
    /**
     * Paragraph `size` affects the font size and spacing.
     * Moving forward, please use `large` or leave out to use the default style.
     *
     * Numerical string values are deprecated and will be removed in the next release.
     */
    size?: 'large' | '100' | '150' | '200' | '250';
    style?: React.CSSProperties;
    /**
     * Optional tag to use as root of Paragraph element.
     * @default `p` tag
     */
    tag?: keyof React.ReactHTML;
    /**
     * String that will be used as a data-testid attribute for automation selection
     */
    testid?: string;
    /**
     * Allows you to set the text color of your headings for theming paragraphs.
     * The value should correlate with the theme of the parent.
     * Defaults to `light` theme (i.e. dark text color).
     *
     * @default light
     */
    theme?: 'light' | 'dark';
}
export declare const Paragraph: React.FC<ParagraphProps>;
