import * as React from 'react';
export interface InputAddonProps {
    /**
     * Adds an element before the input
     */
    append?: React.ReactNode;
    /**
     * Adds an element after the input
     */
    prepend?: React.ReactNode;
}
interface InputAddonFullProps extends InputAddonProps {
    hasErrors?: boolean;
    focused?: boolean;
    hovered?: boolean;
    testid?: string;
    className?: string;
}
interface InputAddonState {
    isHovered: boolean;
    isFocused: boolean;
}
export declare class InputAddon extends React.PureComponent<InputAddonFullProps, InputAddonState> {
    state: {
        isHovered: boolean;
        isFocused: boolean;
    };
    handleFocus: () => void;
    handleBlur: () => void;
    handleMouseOver: () => void;
    handleMouseOut: () => void;
    render(): {};
}
export {};
