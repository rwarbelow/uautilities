import * as React from 'react';
import { InputErrorProps, InputChangeEvent } from '../../types/InputTypes';
interface CheckboxGroupProps extends InputErrorProps {
    className?: string;
    /**
     * Array of strings to that indicate which are disabled
     */
    disabledValues?: string[];
    /**
     * Caption for fieldset
     */
    groupLabel: string;
    /**
     * Function that passes the input click event
     */
    handleSetMultiChecked: (e: InputChangeEvent) => void;
    /**
     * Additional information that is displayed in a tooltip next to the input label.
     */
    additionalInfo?: React.ComponentType | string;
    name: string;
    /**
     * Bool to disable all checkbox inputs
     */
    readOnly?: boolean;
    /**
     * Array of strings to that indicate which are checked
     */
    selectedValues?: string[];
    /**
     * Array of objects used to create the checkbox inputs
     */
    values: {
        value: string | number;
        label: string;
    }[];
}
export declare const CheckboxGroup: React.FC<CheckboxGroupProps>;
export {};
