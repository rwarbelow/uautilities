/**
 * @description
 * This helper normalizes scrollIntoView behavior be falling back to `scrollTop` maniulation for IE11 so that
 * the entire browser does not jump. Modern browsers support options for `scrollIntoView` which allow for more nuanced
 * behavior of how elements scroll into view.
 *
 * IE11 is not a modern browser, and the default behavior causes the entire page to scroll
 * @see https://stackoverflow.com/questions/11039885/scrollintoview-causing-the-whole-page-to-move
 *
 * @param options Optional options for `scrollIntoView` that is only applied to browsers that support this extra functionality
 * @see https://developer.mozilla.org/en-US/docs/Web/API/Element/scrollIntoView#Browser_compatibility
 */
export declare function scrollElementInView(target: HTMLElement, options?: ScrollIntoViewOptions): void;
