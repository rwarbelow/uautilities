import classNames from '../../utilities/classnames';
import * as React from 'react';
import { progressBar, progress, animated } from './index.module.css.js';
export var ProgressBar = function (props) {
    var className = props.className, currentProgress = props.currentProgress, totalProgress = props.totalProgress, animate = props.animate;
    var progressWidth = (currentProgress / totalProgress) * 100 + '%';
    var progressClasses = animate ? classNames(animated, progress) : progress;
    return (React.createElement("div", { className: classNames(progressBar, className), "data-testid": props.testid, role: 'progressbar', "aria-valuenow": currentProgress, "aria-valuemin": 0, "aria-valuemax": totalProgress },
        React.createElement("div", { className: progressClasses, style: { width: progressWidth } })));
};
