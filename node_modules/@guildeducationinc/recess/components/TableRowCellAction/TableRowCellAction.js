import * as React from 'react';
import { cellActionable } from './index.module.css.js';
import classNames from '../../utilities/classnames';
import { PopoverMenu } from '../PopoverMenu';
import { IconButton } from '../Button';
export var TableRowCellAction = function (props) {
    var _a = props.type, type = _a === void 0 ? 'text' : _a, rowIndex = props.rowIndex;
    return (React.createElement("td", { className: classNames(cellActionable, type), "data-testid": '_table-cell-action', style: {
            minWidth: props.minWidth,
            maxWidth: props.maxWidth,
            width: props.width,
            flex: props.flex + " " + props.flex + " " + (props.flex / props.flexBasisMultiplier) * 100 + "%",
        } },
        React.createElement(PopoverMenu, { data: props.row, actions: props.actions, id: "table-row-action-menu-" + rowIndex, testid: "_table-cell-action-item-" + rowIndex },
            React.createElement(IconButton, { label: 'Row Actions', appearance: 'text', iconName: 'MoreVertical', testid: 'button' }))));
};
