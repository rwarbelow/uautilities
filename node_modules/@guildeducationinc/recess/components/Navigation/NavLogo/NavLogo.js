import * as React from 'react';
import { detectIE } from '../../../utilities/detectIE';
import { breakpoints } from '../../../styles/variables/breakpoints';
import { navLogo } from './index.module.css.js';
import { LOGOS_G_ADMIN } from '../../../constants/guild-logos';
export var NavLogo = function (_a) {
    var altText = _a.altText, employerNickname = _a.employerNickname, className = _a.className, testid = _a.testid, _b = _a.theme, theme = _b === void 0 ? 'light' : _b, _c = _a.isAdmin, isAdmin = _c === void 0 ? false : _c;
    var finalAltText = altText || (employerNickname ? employerNickname + " and Guild Education" : 'Guild Education');
    var imagePath;
    var baseLogoPath = 'https://recess-images.guildeducation.com/cobranded_logos/enterprise_partners';
    var logoStyle = theme === 'dark' ? 'white' : 'color';
    var extension = detectIE() ? 'png' : 'svg';
    var gPath = "https://recess-images.guildeducation.com/guild_logos/g/g_color." + extension;
    var cobrandPath = baseLogoPath + "/" + employerNickname + "/" + employerNickname + "_cobranded_" + logoStyle + "." + extension;
    var adminPath = LOGOS_G_ADMIN.g_admin_color + ".svg";
    if (isAdmin) {
        imagePath = adminPath;
    }
    else if ((typeof window !== 'undefined' && window.innerWidth < Number.parseInt(breakpoints.md)) ||
        !employerNickname) {
        imagePath = gPath;
    }
    else {
        imagePath = cobrandPath;
    }
    return (React.createElement("img", { alt: finalAltText, className: navLogo + " " + className, "data-testid": testid, src: imagePath }));
};
