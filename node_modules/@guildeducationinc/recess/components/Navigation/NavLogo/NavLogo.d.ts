import * as React from 'react';
import { BaseProps } from '../../../types/Recess';
import { Themeable } from '../../Theme/index';
export interface NavLogoProps {
    /**
     * Text for screen readers to convey the meaning of the logo.
     * In addition to the company name, customize this text with the destination of the link
     * @default 'Guild Education'
     **/
    altText?: string;
    /**
     * Add custom classes to the <img /> element
     **/
    className?: string;
    employerNickname?: string;
    /**
     * A value to specify that you want to use the Guild Admin logo.
     * This will override all other
     */
    isAdmin?: boolean;
}
export declare const NavLogo: React.SFC<NavLogoProps & BaseProps & Themeable>;
