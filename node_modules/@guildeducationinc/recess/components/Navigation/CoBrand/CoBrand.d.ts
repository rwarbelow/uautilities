import * as React from 'react';
export interface CoBrandProps {
    admin?: boolean;
    employerNickname?: string;
    /**
     * Keep in mind the text alternative for the image should convey the action that will
     * be initiated (the purpose of the image), rather than a description of the image.
     * @default 'Guild Home'
     */
    alt?: string;
    testid?: string;
}
export declare const CoBrand: React.SFC<CoBrandProps>;
