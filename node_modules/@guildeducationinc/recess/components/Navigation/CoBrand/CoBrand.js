import * as React from 'react';
import { LOGOS_G_ADMIN } from '../../../constants/guild-logos';
import { ThemeContext } from '../../Theme';
import { Deprecated } from '../../Deprecated';
function getNavLogo(employerNickname, themeName) {
    var baseLogoPath = 'https://recess-images.guildeducation.com/enterprise_guild_cobranded_logos';
    return baseLogoPath + "/" + themeName + "/standard_size/" + employerNickname + "_" + themeName + ".svg";
}
export var CoBrand = function (_a) {
    var employerNickname = _a.employerNickname, admin = _a.admin, alt = _a.alt, testid = _a.testid;
    var contextTheme = React.useContext(ThemeContext);
    var nickname = employerNickname || 'guild';
    var computedTheme = (contextTheme === null || contextTheme === void 0 ? void 0 : contextTheme.theme) === 'dark' ? 'dark_theme' : 'color';
    var src = admin ? LOGOS_G_ADMIN.g_admin_color + ".svg" : getNavLogo(nickname, computedTheme);
    return (React.createElement(Deprecated, { currentComponent: 'CoBrand', newComponent: 'NavLogo' },
        React.createElement("img", { "data-testid": testid, src: src, alt: alt || 'Guild Home' })));
};
