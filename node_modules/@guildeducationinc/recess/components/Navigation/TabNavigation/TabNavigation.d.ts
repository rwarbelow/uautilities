import * as React from 'react';
import { Themeable } from '../../Theme';
export interface TabNavigationProps extends Themeable {
    /**
     * A brief description of the purpose of the navigation, omitting the
     * term "navigation", as the screen reader will read both the role and
     * the contents of the label. Examples: "Primary", "Secondary", "Footer", etc.
     */
    ariaLabel: string;
    /**
     * Custom class names
     */
    className?: string;
    /**
     * Used as a data-testid attribute for testing
     */
    testid: string;
    /**
     * Optionally arrange the tabs vertically
     */
    vertical?: boolean;
}
export declare class TabNavigation extends React.PureComponent<TabNavigationProps & {
    forwardedRef?: React.RefObject<HTMLElement>;
}> {
    render(): JSX.Element;
}
