import * as React from 'react';
import { PopoverMenu } from '../../PopoverMenu/PopoverMenu';
import { BaseButton } from '../../Button/BaseButton';
import * as navbarStyles from '../NavBar/index.module.css.js';
export var TabDropdown = function (_a) {
    var actions = _a.actions, children = _a.children, className = _a.className, dropdownId = _a.dropdownId, onClick = _a.onClick;
    return (React.createElement("li", { className: className },
        React.createElement(PopoverMenu, { actions: actions, id: dropdownId },
            React.createElement(BaseButton, { className: navbarStyles.navDropdown, iconDirection: 'right', iconName: 'ChevronDown', onClick: onClick }, children))));
};
TabDropdown.displayName = 'TabDropdown';
