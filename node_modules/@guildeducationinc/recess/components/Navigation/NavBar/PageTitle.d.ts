import * as React from 'react';
interface AnchorRoute {
    href: string;
    name: string;
    current?: boolean;
}
interface NavRoute {
    to: string;
    name: string;
    current?: boolean;
}
interface BaseOnBackOptions {
    title?: string;
    onBack?: () => void;
}
interface BaseOnBackOptions {
    title?: string;
    onBack?: () => void;
}
interface OnBackHref {
    href?: string;
}
interface OnBackRoute {
    to?: string;
}
declare type OnBackOptions = (BaseOnBackOptions & OnBackHref) | (BaseOnBackOptions & OnBackRoute);
declare type BreadcrumbRoute = AnchorRoute | NavRoute;
export interface PageTitleProps {
    /**
     * Breadcrumb routes that will display on desktop only.
     * Giving the route an href will use a regular a tag and represents an exernal page.
     * Giving the route a to prop will use a react router link component and should only be used in an application using react router.
     */
    routes?: BreadcrumbRoute[];
    /**
     * Options for how a back navigation button should behave and display, including
     * 1. title -- Button Label
     * 2. onBack -- callback called when button is clicked
     * 3. href/to -- external link or react router path to navigate to when clicked
     */
    onBackOptions?: OnBackOptions;
    /**
     * Semantic HTML tag for page title
     * @default h1
     */
    tag?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
    /**
     * Used as a data-testid attribute for selection
     */
    testid?: string;
    /**
     * Page header that will represent the page topic
     */
    title?: string;
}
export declare const PageTitle: React.SFC<PageTitleProps>;
export {};
