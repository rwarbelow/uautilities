import * as React from 'react';
import { BaseProps } from '../../../types/Recess';
import { Themeable } from '../../Theme/index';
interface NavBarProps<P> extends BaseProps, Themeable {
    /**
     * Custom class names
     */
    className?: string;
}
export declare function NavBar<P>({ children, testid, theme: passedTheme, }: React.PropsWithChildren<NavBarProps<P>>): JSX.Element;
export {};
