import * as React from 'react';
import { Actions } from '../../MenuList/MenuList';
export interface UserActionProps<P> {
    /**
     * Boolean that distinguises between showing the logged in actions dropdown or the logged out action buttons
     *
     * @default false
     */
    loggedIn?: boolean;
    /**
     * string that will serve as the title of the logged in user action button
     */
    loggedInTitle?: string;
    /**
     * Callback which fires when the primary logged out action is clicked
     */
    onPrimaryClick?: React.EventHandler<React.MouseEvent<HTMLButtonElement>>;
    /**
     * Optional href to navigate to when the primary button is clicked
     */
    primaryHref?: string;
    /**
     * Optional title that allows for customization of the primary logged out action button
     * @default "Sign Up"
     */
    primaryTitle?: string;
    /**
     * Optionally hide the primary logged out action button
     * @default false
     */
    hidePrimaryAction?: boolean;
    /**
     * Callback which fires when the secondary logged out action is clicked
     */
    onSecondaryClick?: React.EventHandler<React.MouseEvent<HTMLButtonElement>>;
    /**
     * Optional href to navigate to when the secondary button is clicked
     */
    secondaryHref?: string;
    /**
     * Optional title that allows for customization of the secondary logged out action button
     * @default "Sign in"
     */
    secondaryTitle?: string;
    /**
     * Optionally hide the secondary logged out action button
     * @default false
     */
    hideSecondaryAction?: boolean;
    /**
     * Set of user actions that can be take when a user is logged in.
     *
     * @default []
     */
    loggedinActions?: Actions<void, P>;
    /**
     * Callback which fires when the user action trigger button is clicked
     */
    onUserActionsClick?: (e?: React.MouseEvent<HTMLButtonElement>) => void;
    /**
     * Used as a data-testid attribute for selection
     */
    testid?: string;
}
export declare function UserActions<P>({ loggedIn, onUserActionsClick, loggedInTitle, loggedinActions, primaryTitle, onPrimaryClick, primaryHref, hidePrimaryAction, secondaryTitle, onSecondaryClick, secondaryHref, hideSecondaryAction, testid, }: React.PropsWithChildren<UserActionProps<P>>): JSX.Element;
export declare namespace UserActions {
    var displayName: string;
    var defaultProps: {
        loggedinActions: any[];
        primaryTitle: string;
        hidePrimaryAction: boolean;
        secondaryTitle: string;
        hideSecondaryAction: boolean;
    };
}
