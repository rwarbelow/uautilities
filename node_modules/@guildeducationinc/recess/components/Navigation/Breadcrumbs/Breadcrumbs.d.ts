import * as React from 'react';
import { BaseProps } from '../../../types/Recess';
export interface BreadcrumbsProps extends BaseProps {
    /**
     * Custom label that will be read out to screenreaders to identify the breadcrumb component.
     *
     * @default Breadcrumb
     */
    ariaLabel?: string;
}
export declare const Breadcrumbs: React.SFC<BreadcrumbsProps>;
