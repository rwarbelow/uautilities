var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
import * as React from 'react';
import { ChevronRight } from '../../Icons';
import classNames from 'classnames';
import { breadcrumbs } from './index.module.css.js';
export var Breadcrumbs = function (_a) {
    var children = _a.children, ariaLabel = _a.ariaLabel, className = _a.className, testid = _a.testid;
    var links = React.Children.toArray(children);
    var firstLinks = links.slice(0, -1);
    var lastLink = links[React.Children.count(children) - 1];
    var linksWithDelinator = firstLinks.reduce(function (acc, link, currentIndex) {
        acc.push(link);
        acc.push(React.createElement(ChevronRight, { "aria-hidden": 'true', key: currentIndex }));
        return acc;
    }, []);
    return (React.createElement("nav", { "data-testid": testid, "aria-label": ariaLabel || 'Breadcrumb', className: classNames(breadcrumbs, className) }, __spreadArrays(linksWithDelinator, [lastLink])));
};
