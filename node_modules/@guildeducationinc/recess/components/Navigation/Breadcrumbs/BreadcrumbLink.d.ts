import * as React from 'react';
export interface BreadcrumbLinkProps {
    /**
     * URL for where the given  `BreadCrumb` link navigates to. Will use an underlying `a` tag
     */
    href?: string;
    /**
     * URL for where the given  `BreadCrumb` link navigates to. Mimics the React Router `Link` API and will
     * use a react-router connected `Link` component under the hood.
     */
    to?: string;
    /**
     * This prop indicates if the link represents the current page. This prop **should** be added when necessary for
     * accessibility purposes.
     */
    current?: boolean;
    /**
     * Optional classnames for custom styles
     */
    className?: string;
    testid?: string;
}
export declare const BreadcrumbLink: React.SFC<BreadcrumbLinkProps>;
