import * as React from 'react';
import { Themeable } from '../../Theme/index';
import { BaseProps } from '../../../types/Recess';
export declare enum Position {
    fixed = "fixed",
    absolute = "absolute",
    sticky = "sticky"
}
export interface AppBarProps {
    /**
     * The positioning type. The behavior of the options is described in
     * [the MDN web docs](https://developer.mozilla.org/en-US/docs/Web/CSS/position).
     *
     * **NOTE**: `position: sticky` is not supported to IE11, and will default back to `position: static`
     * @default static
     */
    position?: Position;
    /**
     * @default header
     */
    Element?: keyof React.ReactHTML;
}
export declare const AppBar: React.SFC<AppBarProps & BaseProps & Themeable>;
