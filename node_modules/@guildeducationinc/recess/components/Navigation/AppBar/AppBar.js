import * as React from 'react';
import classNames from 'classnames';
import { absolute, appBar, fixed, sticky, dark, light } from './index.module.css.js';
import { detectIE } from '../../../utilities/detectIE';
import { ThemeContext } from '../../Theme/index';
export var Position;
(function (Position) {
    Position["fixed"] = "fixed";
    Position["absolute"] = "absolute";
    Position["sticky"] = "sticky";
})(Position || (Position = {}));
export var AppBar = function (props) {
    var _a;
    var contextTheme = React.useContext(ThemeContext);
    var computedTheme = props.theme || (contextTheme === null || contextTheme === void 0 ? void 0 : contextTheme.theme) || 'light';
    var classes = classNames(appBar, computedTheme, (_a = {},
        _a[absolute] = props.position === Position.absolute,
        _a[sticky] = props.position === Position.sticky && detectIE() !== 11,
        _a[fixed] = props.position === Position.fixed,
        _a[dark] = computedTheme === 'dark',
        _a[light] = computedTheme === 'light',
        _a), props.className);
    var _b = props.Element, Element = _b === void 0 ? 'header' : _b;
    return (React.createElement(Element, { className: classes, "data-testid": props.testid }, props.children));
};
