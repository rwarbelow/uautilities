import * as React from 'react';
export interface Theme {
    theme: 'dark' | 'light';
}
export interface Themeable {
    /**
     * Theme applies to the given component and overrides any `ThemeContext` the component may lie within. If no prop or theme conext is present, the theme defaults to light.
  
     */
    theme?: 'light' | 'dark';
}
export declare const ThemeContext: React.Context<Theme>;
export declare const ThemeContextProvider: React.Provider<Theme>;
export declare const ThemeContextConsumer: React.Consumer<Theme>;
