import * as React from 'react';
import { stepperContent } from './index.module.css.js';
import { StepperLink } from './StepperLink';
export var StepperContent = function (_a) {
    var children = _a.children;
    React.useEffect(function () {
        if (children) {
            React.Children.forEach(children, function (child) {
                if (console && child.type !== StepperLink) {
                    console.warn("<Stepper.Content> should only contain <Stepper.Link> children");
                }
            });
        }
    }, []);
    if (!children) {
        return null;
    }
    return (React.createElement("nav", { className: stepperContent },
        React.createElement("ul", null, children)));
};
