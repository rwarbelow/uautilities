import * as React from 'react';
import { FooterColumn as FooterColumnType, FooterSocialLink } from './FooterTypes';
export interface FooterProps {
    className?: string;
    /**
     * Array of any columns you want to add to the footer. The default columns are `Product` and `Guild`.
     * Unless `includeDefaultColumns` is set to false, whatever columns provided will be concatenated with
     * the default columns.
     */
    columns?: FooterColumnType[];
    /**
     * Whether the default columns (Product and Guild) are included in the footer.
     * If kept true, then whatever is provided in the `columns` prop with be concatenated
     * with the default columns.
     *
     * @default true
     */
    includeDefaultColumns?: boolean;
    /**
     * Whether the "light" footer is rendered (i.e. Only the Copyright).
     * Formerly this was it's own component, `FooterLight`.
     *
     * @default false
     */
    isFooterLight?: boolean;
    /**
     * Option to add different Guild logo
     *
     * @default guild_color
     */
    logo?: string;
    /**
     * Add different social links to bottom block of footer
     *
     * @default Facebook, Instagram and Twitter
     */
    socialLinks?: FooterSocialLink[];
}
export declare const Footer: React.SFC<FooterProps>;
