import * as React from 'react';
import { AlignmentPositions } from '../../utilities/positionCalculator';
export interface PanelWrapperConfig {
    /**
     * Wrapper class name to allow for custom styling. This class applies to the outer wrapper div
     */
    wrapperClassName: string;
    /**
     * Optional base arrow class that serves to
     */
    arrowBaseClass?: string;
}
export interface PanelContent {
    positionStyles: React.CSSProperties;
    arrowPosition: string;
    targetPosition?: ClientRect;
    positionName?: AlignmentPositions;
    testid: string;
    ariaId: string;
}
declare function MemoWrapperFactory(config: PanelWrapperConfig): <P extends any>(PanelContent: React.ComponentType<any>) => React.ForwardRefExoticComponent<any>;
export default MemoWrapperFactory;
