import * as React from 'react';
import { Actions } from '../MenuList/MenuList';
export interface MenuProps<T, P> {
    /**
     * Array of action items
     */
    actions: Actions<T, P>;
    /**
     * This data is bound to the click event of an individual popover menu item
     */
    data?: T;
    /**
     * Unique id for PopoverMenu. Important for optimum accessibility as it will be used for aria attributes on trigger and popover panel.
     */
    id: string;
    /**
     * String that will be used as a data-testid attribute for automation selection
     */
    testid?: string;
}
export declare function PopoverMenu<T, P>(props: React.PropsWithChildren<MenuProps<T, P>>): JSX.Element;
