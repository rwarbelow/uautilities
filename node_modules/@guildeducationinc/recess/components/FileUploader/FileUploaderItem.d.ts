import * as React from 'react';
interface FileUploaderItemProps<T> {
    label: React.ReactNode;
    removeHandler: (data: T) => void;
    uploadError?: string;
    uploadProgress?: number;
    file: T;
}
export declare function FileUploaderItem<T>({ label, removeHandler, uploadError, uploadProgress, file, }: FileUploaderItemProps<T>): JSX.Element;
export {};
