import * as React from 'react';
import { IconButton } from '../Button';
import { ProgressBar } from '../ProgressBar';
import { Check, ErrorCircle } from '../Icons';
import classNames from 'classnames';
import { uploadErrors, uploadFileItemIcon, uploadFileItemError, uploadFileItemLabel, } from './index.module.css.js';
export function FileUploaderItem(_a) {
    var label = _a.label, removeHandler = _a.removeHandler, uploadError = _a.uploadError, uploadProgress = _a.uploadProgress, file = _a.file;
    var inProgress = !uploadError && uploadProgress !== undefined && uploadProgress < 100;
    return (React.createElement("li", { "data-testid": 'uploadFileItem', className: classNames((uploadError && uploadErrors) || null) },
        (inProgress && (React.createElement(ProgressBar, { totalProgress: 100, currentProgress: uploadProgress !== undefined ? uploadProgress : 1, testid: 'uploadProgress' }))) ||
            null,
        (!inProgress && uploadError && (React.createElement("span", { className: uploadFileItemIcon },
            React.createElement(ErrorCircle, { size: 16, color: '#ff314f' })))) ||
            null,
        (!inProgress && !uploadError && (React.createElement("span", { className: uploadFileItemIcon },
            React.createElement(Check, { size: 16 })))) ||
            null,
        (!inProgress && (React.createElement("span", { className: uploadFileItemLabel, "data-testid": 'uploadLabel' }, label))) ||
            null,
        (!inProgress && uploadError && (React.createElement("span", { className: uploadFileItemError, "data-testid": 'uploadError' },
            React.createElement("strong", null,
                "Error: ",
                uploadError)))) ||
            null,
        React.createElement(IconButton, { appearance: 'text', iconName: 'Cancel', testid: 'removeUpload', label: 'Cancel Upload', onClick: function () { return removeHandler(file); }, "aria-label": 'Remove file' })));
}
