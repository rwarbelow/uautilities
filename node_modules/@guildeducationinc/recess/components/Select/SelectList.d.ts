import * as React from 'react';
import { SelectOption } from './Select';
interface SelectListProps {
    focusedIndex: number;
    handleChange: ({ index, checked }: {
        index: number;
        checked: boolean;
    }) => void;
    handleKeyboardNav: (e: React.KeyboardEvent) => void;
    id?: string;
    labelledBy?: string;
    onListClose: () => void;
    options: SelectOption[];
    selectedValue: string[];
}
export declare const SelectList: ({ focusedIndex, handleChange, handleKeyboardNav, id, labelledBy, onListClose, options, selectedValue, }: SelectListProps) => JSX.Element;
export {};
