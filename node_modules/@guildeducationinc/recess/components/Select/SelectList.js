import * as React from 'react';
import { listOptions } from './index.module.css.js';
import { SelectItem } from './SelectItem';
export var SelectList = function (_a) {
    var focusedIndex = _a.focusedIndex, handleChange = _a.handleChange, handleKeyboardNav = _a.handleKeyboardNav, id = _a.id, labelledBy = _a.labelledBy, onListClose = _a.onListClose, options = _a.options, selectedValue = _a.selectedValue;
    React.useEffect(function () {
        return function () {
            onListClose();
        };
    }, []);
    return (React.createElement("ul", { id: id, "aria-labelledby": labelledBy, role: 'listbox', className: listOptions, tabIndex: 0, onKeyDown: handleKeyboardNav, "data-testid": '_select-menu-list' }, options.map(function (option, idx) {
        var checked = !!(selectedValue.toString() === option.value.toString());
        return (React.createElement(SelectItem, { index: idx, focused: idx === focusedIndex, key: "" + option.value, checked: checked, disabled: option.disabled, toggleChecked: handleChange, option: option }));
    })));
};
