import * as React from 'react';
import { TabPaneProps } from './TabPane';
import { TabProps } from './Tab';
export interface TabsProps {
    activeIndex?: number;
    border?: boolean;
    className?: string;
    onActive?: (index: number) => void;
}
export interface TabState {
    activeIndex?: number;
}
export declare class Tabs extends React.Component<TabsProps, TabState> {
    private tabCount;
    constructor(props: TabsProps);
    static Pane: React.FunctionComponent<TabPaneProps>;
    static Tab: React.FunctionComponent<TabProps>;
    static getDerivedStateFromProps(nextProps: TabsProps, prevState: TabState): {
        activeIndex: number;
    };
    activateTab: (index: number) => void;
    keyDownHandler(event: React.KeyboardEvent<HTMLElement>): void;
    render(): JSX.Element;
}
