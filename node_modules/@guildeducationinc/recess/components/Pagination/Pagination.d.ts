import * as React from 'react';
import { BaseProps } from '../../types/Recess';
export interface PaginationProps extends BaseProps {
    /**
     * Integer to determine current active page
     */
    currentPage: number;
    /**
     * Integer to determine total number of pages
     */
    totalPages: number;
    /**
     * Function to be called when page item is clicked
     */
    onChangePage: (page: number) => void;
    /**
     * Boolean that toggle slim pagination. Defaults to true
     */
    isSlim?: boolean;
    testid?: string;
}
export declare class Pagination extends React.PureComponent<PaginationProps, {}> {
    render(): JSX.Element;
    private changePage;
    private decrementPage;
    private incrementPage;
    private pageRange;
}
