import { BaseProps } from '../../types/Recess';
export interface ElaboratePaginationProps extends BaseProps {
    /**
     * Integer to determine current active page
     */
    currentPage: number;
    /**
     * Integer to determine total number of pages
     */
    totalPages: number;
    /**
     * Function to be called when page item is clicked
     */
    onChangePage: (page: number) => void;
    testid?: string;
}
export declare const sortedPages: (currentPage: number, totalPages: number, pageVariance: number, boundaryLimit: number) => any[];
export declare const ElaboratePagination: (props: ElaboratePaginationProps) => JSX.Element;
export default ElaboratePagination;
