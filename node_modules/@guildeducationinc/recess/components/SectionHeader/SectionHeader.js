import React from 'react';
import PropTypes from 'prop-types';
import { sectionHeader, sectionHeaderButton } from './index.module.css.js';
export function SectionHeader(props) {
    var onButtonClick = props.onButtonClick, title = props.title;
    return (React.createElement("div", { className: sectionHeader },
        onButtonClick ? (React.createElement("button", { onClick: onButtonClick, type: 'button', className: sectionHeaderButton })) : (React.createElement("div", null)),
        React.createElement("strong", null, title),
        React.createElement("div", null)));
}
SectionHeader.propTypes = {
    onButtonClick: PropTypes.func,
    title: PropTypes.string,
};
