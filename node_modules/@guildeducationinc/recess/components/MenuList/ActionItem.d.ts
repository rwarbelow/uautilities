import { MenuAction } from './MenuItem';
import { BaseProps } from '../../types/Recess';
export interface LinkItemProps<T> extends MenuAction<T>, BaseProps {
    /**
     *  Boolean value indicating whether the menu item is currently in focus or not
     */
    focused: boolean;
    /**
     *  Data to be bound to click and keyboard events
     */
    data: T;
}
declare function ActionItem<T>(props: LinkItemProps<T>): JSX.Element;
export default ActionItem;
