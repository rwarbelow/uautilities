import { BaseLinkAction, CustomLinkAction } from './MenuItem';
import { BaseProps } from '../../types/Recess';
export interface BaseLinkItemProps<T> {
    /**
     *  Boolean value indicating whether the menu item is currently in focus or not
     */
    focused: boolean;
    /**
     *  Data to be bound to click and keyboard events
     */
    data: T;
    /**
     *  classNames to apply to containing li
     */
    parentClassName: string;
}
declare type CustomLinkItemProps<T, P> = BaseLinkItemProps<T> & CustomLinkAction<T, P>;
declare type LinkItemProps<T> = BaseLinkItemProps<T> & BaseLinkAction<T>;
declare function LinkItem<T, P>(props: LinkItemProps<T> & CustomLinkItemProps<T, P> & BaseProps): JSX.Element;
export default LinkItem;
