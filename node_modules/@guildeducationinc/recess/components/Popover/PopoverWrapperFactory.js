import PanelContentWrapperFactory from '../Panel/PanelContentWrapperFactory';
import { popover, popoverArrow, dark } from './index.module.css.js';
import classNames from '../../utilities/classnames';
export var PopoverWrapperFactory = function (isDark) {
    var _a, _b;
    if (isDark === void 0) { isDark = false; }
    return PanelContentWrapperFactory({
        wrapperClassName: classNames(popover, (_a = {}, _a[dark] = isDark, _a)),
        arrowBaseClass: classNames(popoverArrow, (_b = {}, _b[dark] = isDark, _b)),
    });
};
