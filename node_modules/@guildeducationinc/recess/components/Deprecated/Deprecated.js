import * as React from 'react';
export var Deprecated = function (_a) {
    var children = _a.children, currentComponent = _a.currentComponent, newComponent = _a.newComponent;
    React.useEffect(function () {
        if (console) {
            console.warn("\n        The " + currentComponent + " component is deprecated and will be removed in next major version. Please use " + newComponent + " component instead.\n      ");
        }
    }, []);
    return React.createElement(React.Fragment, null, children);
};
