/// <reference types="cypress" />
/**
 * Navigates to a given page.
 * This helper yields the pagination element.
 *
 * @example
 * ```ts
 *    cy.get('[data-testid="myPaginationComponent]')
 *      .then(goToPage(2))
 * ```
 */
export declare const goToPage: (pageNumber: number) => ($element: JQuery<HTMLElement>) => Cypress.Chainable<JQuery<HTMLElement>>;
/**
 * Navigates to the next page.
 * This helper yields the pagination element.
 *
 * @example
 * ```ts
 *    cy.get('[data-testid="myPaginationComponent]')
 *      .then(nextPage)
 * ```
 */
export declare const nextPage: ($element: JQuery<HTMLElement>) => Cypress.Chainable<JQuery<HTMLElement>>;
/**
 * Navigates to the previous page.
 * This helper yields the pagination element.
 *
 * @example
 * ```ts
 *    cy.get('[data-testid="myPaginationComponent]')
 *      .then(previousPage)
 * ```
 */
export declare const previousPage: ($element: JQuery<HTMLElement>) => Cypress.Chainable<JQuery<HTMLElement>>;
/**
 * Yields the currently selected page.
 *
 * @example
 * ```ts
 *    cy.get('[data-testid="myPaginationComponent]')
 *      .then(currentPage)
 * ```
 */
export declare const getCurrentPage: ($element: JQuery<HTMLElement>) => Cypress.Chainable<JQuery<HTMLElement>>;
