/// <reference types="cypress" />
/**
 * Yields the link to the current page.
 *
 * @example
 * ```ts
 *    cy.get('[data-testid="myNavComponent]')
 *      .then(currentLink)
 * ```
 */
export declare const getCurrentLink: ($element: JQuery<HTMLElement>) => Cypress.Chainable<JQuery<HTMLElement>>;
/**
 * Yield link containing link name.
 *
 * @example
 * ```ts
 *    cy.get('[data-testid="myNavComponent]')
 *      .then(myLink)
 * ```
 */
export declare const getLink: (linkName: string) => ($element: JQuery<HTMLElement>) => Cypress.Chainable<JQuery<HTMLElement>>;
