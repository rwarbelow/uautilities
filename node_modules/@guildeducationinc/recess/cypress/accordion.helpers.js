/**
 *
 * @beta
 *
 * ```ts
 * const a = b;
 * ```
 */
export function toggleSection(sectionName) {
    return function ($element) {
        cy
            .findByDataId('_accordion-title')
            .contains(sectionName)
            .click();
        return cy.wrap($element);
    };
}
