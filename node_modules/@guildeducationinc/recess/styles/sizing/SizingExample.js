import * as React from 'react';
import { ClassTable } from '../../doc/components/ClassList';
var genClass = function (points, base) {
    return points.map(function (num) {
        var className = ".width--" + num + "/" + base;
        var percentage = ((num / base) * 100).toPrecision(2) + "%";
        return {
            className: className,
            property: "width: " + percentage,
        };
    });
};
export var SizingExample = function (_a) {
    var base = _a.base, points = _a.points;
    return React.createElement(ClassTable, { classes: genClass(points, base) });
};
